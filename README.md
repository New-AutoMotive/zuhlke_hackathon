# Zuhlke Hackathon with New AutoMotive

Install `python` requirements from `requirements.txt` with  
`pip install -r requirements.txt`

Launch Jupyter and read the first notebook, `01_Introduction.ipynb`.
`jupyter lab`